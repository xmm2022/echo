# Echo v0.2 Real Emby Release Gate

Date: 2026-06-02

## Environment

- Echo URL: `http://127.0.0.1:18080`
- Emby client server URL: `http://127.0.0.1:18080/emby`
- Upstream Emby URL: `http://127.0.0.1:8096` (`4.9.3.0`)
- OpenList guangyapan URL: `http://127.0.0.1:5247`
- Echo state: `/home/nax/echo-local-gate`
- OpenList data: `/home/nax/echo-sidecar-data`
- Emby media mount: `/home/nax/echo-emby-media`
- Echo build served by `/readyz`: `e37f6f2-dirty`

## Setup Notes

- OpenList was rebuilt locally from `/home/nax/openlist-guangyapan-src` because the previous binary did not expose the 139 CAS restore fields required by this gate.
- Emby local root user was authenticated with a real access token for the gate. The token is stored outside this document and is not recorded here.
- Emby library `EchoGate` points at `/media`.
- Echo user link maps Emby user `de7eeac1bf204c23a422076999aa6086` to Echo user `admin`.
- Echo account pool assignment `admin` -> `yidon` / `139` is enabled after the gate.
- Echo library mapping id `1` is file-specific:
  - Emby prefix: `/media/12th.Fail.2023.2160p.NF.WEB-DL.DDP.5.1.Atmos.H.265.mkv`
  - Echo rel prefix: `12th.Fail.2023.2160p.NF.WEB-DL.DDP.5.1.Atmos.H.265.mkv`
- Mapped Emby item:
  - Item id `6`
  - Path `/media/12th.Fail.2023.2160p.NF.WEB-DL.DDP.5.1.Atmos.H.265.mkv`
  - Echo `file_copies.id=1`, provider `139`, account `yidon`, state `live/healthy`
- Unmapped Emby item:
  - Item id `7`
  - Path `/media/echo-gate-unmapped.mkv`
- External subtitle file was added for mapped item metadata validation:
  - `/home/nax/echo-emby-media/12th.Fail.2023.2160p.NF.WEB-DL.DDP.5.1.Atmos.H.265.srt`

## Required Manual Checks

- [x] PlaybackInfo for a mapped item returns only `/emby/stream/{token}` or `/emby/error/{token}` playable URLs.
  - `GET /emby/Items/6/PlaybackInfo` returned `200`.
  - First source had `Path="echo://mapped/1"`.
  - First source had `DirectStreamUrl="http://127.0.0.1:18080/emby/stream/{token}"`.
  - First source had `AddApiKeyToDirectStreamUrl=false` and `RequiredHttpHeaders={}`.

- [x] PlaybackInfo for a mapped item does not return `/api/restore`, `/api/stream`, upstream Emby stream URLs, upstream signed URLs, Authorization headers, Cookie headers, or X-Emby-Token values.
  - Forbidden match scan against rewritten PlaybackInfo returned `0`.

- [x] Mapped media starts playback in the Echo stream plane.
  - `/emby/stream/{token}` with `Range: bytes=0-1023` returned `206`, `1024` bytes, `Content-Range: bytes 0-1023/21056321195`.

- [x] Seek works at least twice in the same playback session and byte quota follows actual bytes sent.
  - Same Echo stream token/session was used for:
    - `bytes=0-1023` -> `206`, `1024` bytes
    - `bytes=1048576-1050623` -> `206`, `2048` bytes
    - `bytes=2097152-2099199` -> `206`, `2048` bytes
  - `playback_events` recorded matching `bytes_sent` values and `http_status=206`.

- [x] Pause and resume keep the same user/session attribution.
  - HTTP-level pause/resume approximation was validated by repeated range GETs against the same stream token.
  - Events were attributed to Echo user `admin`, library entry `1`, and the same playback session for each token run.
  - A GUI Emby client pause button was not used in this local gate.

- [x] An unmapped item still plays through upstream Emby.
  - Item `7` PlaybackInfo through Echo returned upstream path `/media/echo-gate-unmapped.mkv` and no Echo `/emby/stream` URL.
  - `/emby/Videos/7/stream?Static=true&MediaSourceId=mediasource_7` through Echo returned `206`, `1024` bytes, `Content-Range: bytes 0-1023/28529`.

- [x] Quota exhaustion returns a controlled client-facing error with `X-Echo-Reason: quota_exceeded`.
  - Quota policy id `1` was temporarily patched to `period=day`, `max_bytes=5120`.
  - Next `/emby/stream/{token}` range request returned `429`, `X-Echo-Reason: quota_exceeded`, body `{"error":"quota_exceeded"}`.
  - Policy id `1` was restored to `name=unlimited`, `period=none`, `max_bytes=NULL`.

- [x] Disabled account pool returns a controlled client-facing error with `X-Echo-Reason: account_unavailable` or `temporary_unavailable`.
  - Account pool assignment id `1` was temporarily disabled.
  - `GET /emby/stream/{token}` returned `503`, `X-Echo-Reason: temporary_unavailable`, body `{"error":"temporary_unavailable"}`.
  - `HEAD /emby/stream/{token}` returned `503`, `X-Echo-Reason: temporary_unavailable`.
  - Disabled-pool PlaybackInfo produced an `/emby/error/{token}` URL; dereferencing it returned `503`, `X-Echo-Reason: temporary_unavailable`.
  - Assignment id `1` was restored to enabled.

- [x] Confirmed dead copy falls back to another live copy before response headers are sent.
  - There is only one real 139 cloud copy, so this was validated with a controlled synthetic first candidate and the real live copy as fallback.
  - Temporary fake copy ids `5,6,7` were inserted with missing remote paths; resolver selected copy `7` before the real copy.
  - Copy `7` failed link with confirmed `object_missing` and was marked `dead/confirmed_dead`.
  - The same request still returned `206`, `1024` bytes from the real copy before any error headers were committed.
  - Temporary fake copies and their copy failure row were removed after the probe.

- [!] Suspect `/d/` HTML failure does not permanently mark the copy dead.
  - Not reproduced against the live OpenList instance during this gate.
  - Automated coverage exists for the same failure shape:
    - `internal/sidecarclient`: `/d/` HTML failures classify as transient/suspect.
    - `internal/http/handlers`: HTML 500 stream failures leave copy status `live` and scheduler state `suspect_dead`.
  - This remains a real-hardware follow-up if an OpenList `/d/` HTML 500 can be induced safely.

- [x] Existing subtitle metadata remains present in PlaybackInfo. Subtitle URL proxying is not required for v0.2; unsafe mapped playable subtitle URLs fail closed.
  - Mapped item PlaybackInfo included one external subtitle stream.
  - Subtitle metadata retained: `Type=Subtitle`, `DisplayTitle=(SRT)`, `Codec=srt`, `IsExternal=true`.
  - `DeliveryUrl` and `ExternalUrl` were absent/null after rewrite.
  - Forbidden match scan against subtitle-bearing PlaybackInfo returned `0`.

## Final State

- OpenList `/yidon/movies` lists exactly the restored MKV, not the stale `.cas`.
- Echo `/api/restore/1` returns a 139 direct link.
- Echo `/api/stream/1` with `Range: bytes=0-1023` returns `206` and `1024` bytes.
- Echo account pool assignment id `1` is enabled.
- Echo quota policy id `1` is restored to unlimited.
- `file_copies.id=1` is `live/healthy`, `failure_count=0`.

## Verification Commands

- `go test ./internal/embyproxy -count=1`
- `go test ./internal/playback -count=1`
- `go test ./internal/config -count=1`

Final release verification should also include:

- `go test ./internal/sidecarclient ./internal/http/handlers ./internal/playback ./internal/embyproxy ./internal/config`
